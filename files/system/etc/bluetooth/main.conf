# Bluetooth main configuration
# AutoEnable=true was set by Ansible; keeping it here for the image.
# Note: Hoid-specific — bluetooth auto-power was disabled for hoid in Ansible,
# but set to true for other desktops. Adjust if needed for laptop.
[Policy]
AutoEnable=true
